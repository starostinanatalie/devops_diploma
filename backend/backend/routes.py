from backend import backend
from backend.request import GetPlanetData

@backend.route('/')
@backend.route('/index')
def index('/index'):
    for i in

    return

@backend.route('/refresh', methods=['GET', 'POST'])
def refresh():
    planet =

    return "Hello, World!"